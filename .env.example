# GoHighLevel Inbound Webhook for the booking form (Julian sub-account:
# Automation > Workflows > Inbound Webhook trigger > copy URL here AND into
# the Vercel project env vars).
NEXT_PUBLIC_GHL_WEBHOOK_URL=
