# GoHighLevel Inbound Webhook for the booking form — SERVER-SIDE env var
# (this form posts to /api/contact, which forwards to GHL). Set the SAME
# variable in the Vercel project (Settings > Environment Variables).
GHL_WEBHOOK_URL=
